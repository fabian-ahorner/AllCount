<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bitflake.allcount">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name="com.bitflake.counter.wear.WearConnectionApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->

            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".RecordCountActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".CountActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RecordActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".SensorRecordActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.bitflake.counter.services.WearRecordService" />
        <service android:name="com.bitflake.counter.services.WearCountService" />
        <service android:name=".RecordSensorService" />

        <receiver android:name="com.bitflake.counter.wear.WearMiddlemenReceiver">
            <intent-filter>
                <action android:name="com.bitflake.counter.count.status" />
                <action android:name="com.bitflake.counter.count.control" />
                <action android:name="com.bitflake.counter.count.progress" />
                <action android:name="com.bitflake.counter.record.status" />
            </intent-filter>
        </receiver>
        <service android:name=".NotificationReceiver">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER" />
            </intent-filter>
        </service>
    </application>

</manifest>
